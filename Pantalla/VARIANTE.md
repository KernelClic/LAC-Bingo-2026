# Pantalla — Vegano

Variante tematica Vegano.

Diverge en CODIGO de `*/base`. Los cambios compartidos llegan por cascada
desde `main` -> `base` -> esta variante. Los ajustes propios se hacen en
ramas `feat/...` y se mergean de vuelta AQUI.
